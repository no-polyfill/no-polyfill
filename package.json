{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "tsdown": "^0.21.2"
    }
  },
  "scripts": {
    "u": "bun format ; bun lint ; bun run build",
    "format": "oxfmt",
    "lint": "oxlint",
    "build": "bun run --filter='*' build"
  },
  "devDependencies": {
    "oxfmt": "^0.40.0",
    "oxlint": "^1.54.0",
    "typescript": "^5.9.3"
  }
}
